spring:
  profiles:
    include: oauth
  datasource:
#    url: jdbc:mysql://localhost:3306/planetdhance_test?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    url: jdbc:mysql://i7d201.p.ssafy.io:3306/planetdhance_test?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: ssafy
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
#    database-platform: org.hibernate.dialect.H2Dialect


logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace